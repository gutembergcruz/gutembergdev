import { Box } from "@/Components/Box";
import { Cases } from "@/Components/Cases";

export default function Portifolio() {
//  const meuid =  useRouter();

  return (
    <Box menu={3}>
      <Cases />
    </Box>
  )
}